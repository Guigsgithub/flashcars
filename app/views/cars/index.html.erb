<div class="container">
  <h1>Pick your favorite green car !</h1>
    <div class="index-content d-flex">
      <div class="index-cards">
        <% @cars.each do  |car| %>
            <div class="index-card">
              <% if car.photo.attached? %>
                <%= cl_image_tag car.photo.key, crop: :fill, alt:"car-picture", class:"index-img"%>
              <% else %>
                <img class="index-img" src="https://i.pinimg.com/originals/b3/b0/0d/b3b00d8a4250d30936c290faa10c5f36.jpg" alt="car-picture">
              <% end %>
                <div>
                  <h4 class="pt-2">💸 <%= car.price %> €/day</h4>
                  <h5>👨‍👩‍👦‍👦 <%= car.capacity %> person(s)</h5>
                  <h6>🗺️ <%= car.location %></h6>
                  <h6>👤 <%= link_to User.find(car.user_id).username, user_path(User.find(car.user_id)) %></h6>
                  <%= link_to "Rent this car !", car_path(car), class:"btn btn-primary" %>
                </div>
            </div>
      <% end %>
    </div>
    <div class="map-box">
      <div class="placeholder-map">
      <h1>map</h1>
      </div>
    </div>
  </div>
</div>
