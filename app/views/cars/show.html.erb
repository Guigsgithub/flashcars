<h1>🚙 <%= @car[:model] %> 🚙</h1>
<p>💸 Price : <%= @car[:price] %> 💸</p>
<%= link_to "➕ Book this car", new_car_rental_path(@car) %>
<%= link_to "Delete the Car",
            car_path(@car),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<% if @car.photo.attached? %>
<%= cl_image_tag @car.photo.key, height: 300, width: 400, crop: :fill %>
<% end %>